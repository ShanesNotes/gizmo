The bounty/objective panel — "borders that behave." A coral-ruled dark-glass plate with manuscript title, count, and coral→gold progress.

```jsx
<VerdictBar title="The Claiming" desc="Chase the gold — seal the bounty" count={3} total={5} />
```

Pass an `icon` node for the 34px leading slot. `fill` overrides the count/total progress. `compact` renders a slim single-line pill for the portrait HUD.

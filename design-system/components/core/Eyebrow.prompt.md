Manuscript UPPERCASE label (Cormorant Garamond, +2px track) that frames a value — never stars.

```jsx
<Eyebrow>Illumination</Eyebrow>
<Eyebrow color="var(--clutch)">Clutch · Breath</Eyebrow>
```

Default color is gold-leaf; pass a charge hue (`var(--flow)`, `var(--echo)`…) to tag a system.

# THE OVERHAUL PLAN — Shane's playtest 2 + Fable's controller session (2026-07-07)

Two playthroughs compared. Overlapping findings (both lists): controls never taught,
mission unclear at spawn, doors illegible with no lure, the shop is a combat box with
no shop, first-encounter pressure, economy headless. Shane's deeper cuts accepted in
full below. Standard correction acknowledged: my "gorgeous" was calibrated against our
own greybox — a Hades-reference visual workflow is now a lane.

## LANE 0 — lead, immediate (tonight)
1. **Double music on death→hub** (bug, mine): the defeat one-shot (SEG_06) and the hub
   SANCTUARY zone overlap — the one-shot never suppresses/resumes the zone lane. Fix:
   zone playback holds during the one-shot; resume on its `finished`.
2. **Margin recast** (voice was wrong — needs the Theotokos register: Galadriel,
   the lady in the mist, luminous-maternal, low and unhurried). Regenerate all 14
   lines; keep the Pattern (approved).
3. **SFX regeneration**: melee hit (tin clunk → weighted brass-on-ceramic thud with
   warmth), door open (dungeon stone → clockwork iris / warm chime-and-gears —
   transposed to Gizmo's world, not Hades').

## LANE 1 — combat overhaul to the Hades bar (Fable agent)
Swing FEEL: animation-led attacks (windup/contact/follow-through readable at speed),
special-attack animation, combo cycling; replace the cheap swing wedge + red death
ring with professional greybox VFX (trail arcs, radial shockwave with distortion-style
layering — still budget, but crafted); hit-stop retained. Enemy AI: dynamic movement
(strafe orbits, hesitation, lunge telegraphs, flank bias) — trash is easy but never
static. Balance: Halo-CE vitals model — SHIELD = flat recharging bar (delay then
regen), HEALTH = blocks that tick per hit and never regen in-run; retune Gizmo's
totals down. Spawn scatter: break the ring clusters (jittered arcs, spawn spread).

## LANE 2 — levels 3x (Fable agent)
Rooms ~3x current size with interesting geometry and world mechanics (hazards,
elevation reads, cover that matters); region-flavored layouts per the Meridian
grammar; door presentation with LURE (large reward iconography, glow beckons, frame).
Shop room becomes an actual shop scene (safe, merchant fixture, 3 purchasables from
the existing meta/economy data). Mirror in hub = meta-upgrade surface (data exists).

## LANE 3 — the upgrade fantasy (Fable agent)
"Boon" term transposed (lore lab decides; provisional in-game term from glossary
family). EFFECTS THAT MATTER: visible number-go-up, trade-off options, multiplicative
synergies; rarity dynamics with dopamine — epic/legendary drops get presentation
(color flare, sting, pause-beat). Draft screen integrated into the mystical UI
language. HUD pass toward the world (brass filigree, less flat panel).

## LANE 4 — the opening & the guide (Fable agent)
Cinematic opening: campfire introduction of the guide (who she is), mission statement
(what "it" is, why we keep it), controls taught diegetically + a fading keys card.
Portrait/concept-art panel when an NPC speaks (subtle for the guide after her intro).
Deep setting-the-scene VO moment (the recast voice or a second deep narrator).

## LANE 5 — the Hades visual reference workflow (research, Sonnet)
Scout Hades screenshots/frames; extract what "gorgeous" means operationally (value
contrast, silhouette hierarchy, rim lighting, saturation discipline, UI integration);
deliver a calibration workflow the look lane applies each pass.

## Sequencing
Lane 0 now → Lanes 1-4 as parallel Fable agents (fences below) → research feeds Lane 2/3
styling. Ship in waves as lanes green; Shane feel-tests each wave.

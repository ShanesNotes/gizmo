# Architecture / decision records

One file per locked decision: `NNNN-short-title.md` (increment `NNNN`). Keep each tight —
the decision, the **why**, and what it **rules out**. Recorded via the `domain-modeling` skill.

`CONTEXT.md` is the keystone; if an ADR and `CONTEXT.md` ever disagree, reconcile here and
fix the keystone. ADRs predating the 2026-06-20 reset to a 3D rogue-lite live in git history.
